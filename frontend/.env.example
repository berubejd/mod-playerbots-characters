# Development proxy settings (used by `npm run dev` only).
# In production, the C++ HTTP server serves both frontend and API from the same origin.

# VITE_DEV_API_URL=http://127.0.0.1:8501
# VITE_DEV_WS_URL=ws://127.0.0.1:8501
